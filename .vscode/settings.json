{
    "cmake.configureSettings": {
        "CUDAToolkit_ROOT": "${env:HOME}/cuda-12.4",
        "PKG_CONFIG_PATH": "${env:HOME}/.local/lib/pkgconfig/",
        "CMAKE_LIBRARY_PATH": "${env:HOME}/.local/lib"
    },
    "cmake.environment": {
        "PATH": "${env:HOME}/cuda-12.4/bin:${env:PATH}",
        "LD_LIBRARY_PATH": "${env:HOME}/.local/lib:${env:HOME}/cuda-12.4/lib64:${env:LD_LIBRARY_PATH}"
    },
    "C_Cpp.errorSquiggles": "enabled",
    "terminal.integrated.autoReplies": {
        "Build finished with exit code 0": "close"
    }
}
